<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <!--Create a layer list and add item of shape "Rectangle to it"-->
    <item
        android:left="-5dp"
        android:right="-5dp"
        android:top="-5dp"
        android:end="240dp">
        <shape
            android:shape="line">
            <!--Give the dashed lines their property
                 like color, width, and dashGap-->
            <stroke
                android:color="@color/material_blue_grey_500"
                android:dashWidth="10dp"
                android:dashGap="6dp"
                android:width="2dp"
                />
        </shape>
    </item>
    <item
        android:left="-5dp"
        android:right="-5dp"
        android:top="-5dp"
        android:start="230dp">
        <shape
            android:shape="line">
            <!--Give the dashed lines their property
                 like color, width, and dashGap-->
            <stroke
                android:color="@color/material_blue_grey_500"
                android:dashWidth="10dp"
                android:dashGap="6dp"
                android:width="2dp"
                />
        </shape>
    </item>
</layer-list>